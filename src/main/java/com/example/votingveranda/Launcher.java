package com.example.votingveranda;

public class Launcher {
    public static void main(String[] args) {
        // We call HelloApplication's main method directly instead of using Application.launch
        HelloApplication.main(args);
    }
}